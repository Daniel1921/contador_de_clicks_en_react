Esta aplicación tiene como función explicar conceptos muy basicos de react entre los que encontramos:
* componentes
* props
* useState
* comunicación de props del padre al hijo

para iniciar la app utilice el comando "npm start"
